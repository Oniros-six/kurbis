---
const {
    text,
    color,
    hoverColor
} = Astro.props
---
<button
    class=`w-fit self-center bg-[var(${color})] hover:bg-[var(${hoverColor})] text-white font-semibold
                     py-2 px-8 rounded-full transition-all duration-200
                     transform hover:scale-105 active:scale-95
                     shadow-md hover:shadow-lg`
>
    {text}
</button>
